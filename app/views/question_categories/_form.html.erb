<%= form_with(model: question_category) do |form| %>
  <% if question_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(question_category.errors.count, "error") %> prohibited this question_category from being saved:</h2>

      <ul>
        <% question_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-2">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name,  class: 'form-control' %>
  </div>

  <div class="col-md-1">
    <%= form.label :status, style: "display: block" %>
    <%= form.check_box :active, class: 'form-control' %>
  </div>

  <div class="col-md-1">
    <br><%= form.submit 'Save',class: "btn btn-primary" %>
  </div>
<% end %>
